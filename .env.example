# Copy this to .env and fill in your values
# NEVER commit the real .env to version control

DOCKER_HUB_USER=yourdockerhubuser

DB_ROOT_PASS=changeme_rootpass
DB_USER=appuser
DB_PASS=changeme_apppass

IMAGE_TAG=latest
